using System;
using System.Collections.Generic;
using System.Linq;

namespace _03.LegendaryFarming
{
    class Program
    {
        static void Main(string[] args)
        {

            string[] arr = Console.ReadLine().ToLower().Split().ToArray();

            for (int i = 0; i < arr.Length; i++)
            {

            }











            //Dictionary<string, int> dic = new Dictionary<string, int>();

            //string[] line = Console.ReadLine().ToLower().Split().ToArray();

            //List<string> material = new List<string>();
            //List<int> quantity = new List<int>();

            //for (int i = 1; i <= line.Length; i++)
            //{
            //    if(i % 2 == 0)
            //    {
            //        material.Add(line[i]);

            //    }
            //    else
            //    {
            //        quantity.Add(int.Parse(line[i]));
            //    }
            //}

            //for (int i = 0; i < material.Count; i++)
            //{
            //    if(dic.ContainsKey(material[i]))
            //    {
            //        dic[material[i]] += quantity[i];
            //    }
            //    else
            //    {
            //        dic.Add(material[i], quantity[i]);
            //    }
            //}

            //if(dic.ContainsKey("shards"))
            //{
            //    if(dic("Shards"))
            //}
        }
    }
}
